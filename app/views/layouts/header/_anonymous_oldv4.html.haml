%nav.top-bar
  %ul.title-area
    %li.name
      = link_to image_tag("logo-color.png"), home_path, :id => 'logo', :class => 'brand'
    %li.toggle-topbar.menu-icon
      = link_to "/" do
        %span Menu
  %section.top-bar-section
    %ul.right
      = nav_link "Employees", employee_path
      = nav_link "For Business", store_path
      %li.has-form
        = link_to("Sign In", new_customer_session_path, :class => 'radius small button' )