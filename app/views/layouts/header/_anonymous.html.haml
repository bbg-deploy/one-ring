.row
  .twelve.columns
    .fixed
      %nav.top-bar
        %ul
          %li.logo
            = link_to image_tag("logo-color.png"), home_path, :id => 'logo', :class => 'brand'
          %li.toggle-topbar
            = link_to("", "#")
        %section
          %ul.right
            = nav_link "Employee Home", employee_path
            = nav_link "For Business", store_path
            %li.has-button
              = link_to("Sign In", new_customer_session_path, :class => 'radius button' )