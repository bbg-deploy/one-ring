= provide(:title, "New SSO Client")

.row
  .eight.columns
    %h2 New Single Sign-On Client
    = form_for(@client, :url => employee_clients_path, :method => :post, :html => { :class => "" }) do |f|
      %fieldset
        %legend Client Information
        .row
          .eight.columns
            = f.label "Name"
            = f.text_field :name, :placeholder => "Lease Application"
          .four.columns
            %p.helper-text
              = "Must be greater than 6 characters"
        .row
          .eight.columns 
            = f.label "Application ID"
            = f.text_field :app_id, :placeholder => "S3cuREdLeASe"
            = f.text_field :app_id_confirmation, :placeholder => "Enter Application ID Again"
          .four.columns
            %p.helper-text
              = "Must be greater than 6 characters"
      = f.submit "Create Application Token", :class => "radius button"